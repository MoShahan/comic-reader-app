import type { AppSettings } from '@/types/comic';

export const DEFAULT_SETTINGS: AppSettings = {
  themeMode: 'system',
  autoSplitSpreads: true,
  defaultFit: 'screen',
  defaultDirection: 'ltr',
};
